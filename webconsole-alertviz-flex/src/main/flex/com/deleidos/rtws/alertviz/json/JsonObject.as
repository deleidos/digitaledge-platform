/**
 * This is simply a marker class that extends array for ordering items
 * within a JSON object, and to distinquish it from a regular Array.
 */
package com.deleidos.rtws.alertviz.json
{
	public class JsonObject extends Array
	{
		public function JsonObject()
		{
			super();
		}
	}
}