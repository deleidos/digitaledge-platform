CREATE TABLE "DIMENSIONS"."BANK"
(
   ID decimal(65535,32767) PRIMARY KEY NOT NULL,
   NAME varchar(128) NOT NULL
)
;
CREATE UNIQUE INDEX PRIMARY_KEY_6A ON "DIMENSIONS"."BANK"(ID)
;

CREATE TABLE "DIMENSIONS"."TELLER"
(
   ID decimal(65535,32767) PRIMARY KEY NOT NULL,
   NAME varchar(128)
)
;
CREATE UNIQUE INDEX PRIMARY_KEY_85 ON "DIMENSIONS"."TELLER"(ID)
;

CREATE TABLE "DIMENSIONS"."BRANCH"
(
   ID decimal(65535,32767) PRIMARY KEY NOT NULL,
   NAME varchar(128),
   CITY varchar(128) NOT NULL,
   STATE varchar(8) NOT NULL
)
;
CREATE UNIQUE INDEX PRIMARY_KEY_81 ON "DIMENSIONS"."BRANCH"(ID)
;

CREATE TABLE "DIMENSIONS"."ACCOUNT"
(
   ID decimal(65535,32767) PRIMARY KEY NOT NULL,
   NAME varchar(128),
   ADDRESS varchar(512),
   CITY varchar(128),
   STATE varchar(8),
   ZIP integer
)
;
CREATE UNIQUE INDEX PRIMARY_KEY_C9 ON "DIMENSIONS"."ACCOUNT"(ID)
;

